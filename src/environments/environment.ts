// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  url_api: 'http://platzi-store.herokuapp.com',
  firebase: {
    apiKey: 'AIzaSyCpZDYTQgVVp733xTKDoy1_UvKwY30-yHM',
    authDomain: 'platzi-store-cf840.firebaseapp.com',
    databaseURL: 'https://platzi-store-cf840.firebaseio.com',
    projectId: 'platzi-store-cf840',
    storageBucket: 'platzi-store-cf840.appspot.com',
    messagingSenderId: '513035669866',
    appId: '1:513035669866:web:84bfa9ee884b06696e5513'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
